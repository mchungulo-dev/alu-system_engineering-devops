#!/usr/bin/env bash
# Installs and configures HAProxy on lb-01 to load balance
# between web-01 and web-02 using round-robin

sudo apt-get update -y
sudo apt-get install -y haproxy

sudo sed -i 's/^ENABLED=.*/ENABLED=1/' /etc/default/haproxy 2>/dev/null || echo "ENABLED=1" | sudo tee -a /etc/default/haproxy

haproxy_config="
global
    log /dev/log    local0
    log /dev/log    local1 notice
    daemon

defaults
    log     global
    mode    http
    option  httplog
    option  dontlognull
    option  forwardfor
    timeout connect 5000
    timeout client  50000
    timeout server  50000

frontend http_front
    bind *:80
    default_backend http_back

backend http_back
    balance roundrobin
    server 7133-web-01 3.89.109.79:80 check
    server 7133-web-02 52.91.73.13:80 check
"

echo "$haproxy_config" | sudo tee /etc/haproxy/haproxy.cfg
sudo systemctl enable haproxy
sudo service haproxy restart
